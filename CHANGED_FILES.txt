Station reachability + wine crate stock display
Diff against commit 88f119d.

  scripts/Interactables/drinks_station.gd
        + get_interaction_point() — snaps the approach point to the nearest
          navigable floor, and + approach_probe_distance (72px)
  scenes/furniture/wine_crate.tscn
        unit_offsets 1 -> 3 stacked positions so stock level is visible
  tests/reachability_probe.{gd,tscn}
        new — measures every station and prop against the live navmesh

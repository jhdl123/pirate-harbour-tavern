Git-friendly diagnostic run system
Diff against f59bf51.

NEW
  systems/diagnostics/service_chain_validator.gd
        Walks Drink → Order → Station → Content → Restock item → Catalogue →
        Storage → Physical display → Staff replenishment → BarCounter →
        Consumption for every orderable drink. Reports exactly where it breaks.
  systems/diagnostics/diagnostic_run_exporter.gd
        Writes debug/latest and debug/archive/<stamp>, reads the Git commit
        straight from .git (no Git binary needed), logs stock events.
  tests/diagnostic_export_probe.{gd,tscn}   40 assertions, incl. fault injection
  debug/README.md, debug/latest/*           a first real run, committable

MODIFIED
  systems/customer_ai/diagnostics/customer_ai_report_manager.gd
        + get_summary() / get_group_summary() / get_solo_summary() — read the
          same builder the JSON export uses, so they cannot disagree
  systems/staff/diagnostics/staff_report_manager.gd
        + get_summary() reading TaskBoard's own report section
  scripts/UI/stock_dev_panel.gd   + "Export Diagnostic Run" button
  scenes/main/main.tscn           DiagnosticRunExporter node, wired to both
                                  existing managers and to the F10 panel

Bar-side navigation fix (supersedes nav-scan-and-backpressure.zip)
Diff against 9a8e682.

THE FIX
  systems/navigation/navigation_service.gd
        project_to_mesh_from() rewritten. It no longer biases toward the
        worker - that dragged the bar's deposit-side stand across the counter.
        It now samples around the object for a point that is INTERIOR (has
        walkable clearance all round) AND REACHABLE from the actor, nearest
        to the request. Points already standable are returned untouched.

EVERYTHING ELSE carried over from nav-scan-and-backpressure.zip:
  startup navigation scan, per-destination nav telemetry, bar slot budget,
  bottle-station thresholds, plus the sprites/restock work before it.

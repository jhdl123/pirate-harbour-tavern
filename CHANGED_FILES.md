# Changed Files — Phase 3A.1 Staff Refinement

Every file in the update ZIP appears below. Paths are project-relative;
copy them over the existing project preserving the folder structure.

Generated by diffing the delivered tree against the uploaded original, so
this list is derived rather than remembered.

---

## Added (13)

### `CHANGED_FILES.md`
This manifest.

### `Data/staff/carried_item_policy.tres`
The Tavern Hand's carried-item policy instance.

### `Data/staff/task_viability_config.tres`
Tuned viability values, including the retuned rejection margin.

### `PHASE_3A1_STAFF_REFINEMENT.md`
Required documentation for this pass.

### `systems/staff/carried_item_policy.gd`
Resource defining what a worker does with an item left in its hands, and whether it may work while holding one.

### `systems/staff/carried_item_recovery.gd`
Plan/execute service that carries out the policy through real inventory transactions.

### `systems/staff/staff_transition_reasons.gd`
Central StringName constants for every state-change, cancellation and carried-item reason. Replaces the generic "transition" string.

### `systems/staff/tasks/task_viability.gd`
The estimator: turns executor-supplied journey legs into minutes and a verdict.

### `systems/staff/tasks/task_viability_config.gd`
Resource holding every viability margin, weight and estimation setting.

### `tests/phase_3a1_benchmark.gd`
A/B overload benchmark; --legacy reproduces Phase 3A selection.

### `tests/phase_3a1_benchmark.tscn`
Scene wrapper for the benchmark.

### `tests/phase_3a1_refinement_test.gd`
21-check automated suite for the refinement scenarios.

### `tests/phase_3a1_refinement_test.tscn`
Scene wrapper for the refinement suite.

---

## Modified (15)

### `Data/staff/task_board_config.tres`
Wires the viability resource and the commitment defaults.

### `Data/staff/tasks/clean_seat.tres`
carried_item_rule = REQUIRES_EMPTY_HANDS - the data-driven half of the carried-item fix.

### `Data/staff/tasks/serve_drink.tres`
carried_item_rule = REQUIRES_MATCHING_OR_EMPTY; viability_weight = 1.0.

### `Data/staff/tavern_hand.tres`
Wires the carried-item policy.

### `scripts/Entities/customer.gd`
Added get_patience_remaining_minutes(), the deadline viability measures against.

### `systems/staff/diagnostics/staff_report_manager.gd`
Added the state_transitions section grouped by from/to/reason.

### `systems/staff/executors/clean_seat_executor.gd`
Reports no deadline; supplies a travel estimate and the cleaning action's duration.

### `systems/staff/executors/serve_drink_executor.gd`
Implements the patience deadline and the two-leg travel estimate.

### `systems/staff/executors/staff_task_executor.gd`
Added the four estimation hooks and the default is_compatible_with_carried_item() rule.

### `systems/staff/staff_definition.gd`
Added the carried_item_policy reference.

### `systems/staff/staff_member.gd`
Hands-before-work ordering; carried-item recovery driving; new RECOVERING_ITEM state; a reason on every transition; idle oscillation fix; new counters; removed the superseded _begin_returning_carried_item()/_find_free_service_slot() pair.

### `systems/staff/tasks/tavern_task.gd`
Added viability cache, path-estimate cache, invested_minutes and release provenance; exposed them in to_dictionary().

### `systems/staff/tasks/tavern_task_board_config.gd`
Added viability, commitment and decision-logging settings.

### `systems/staff/tasks/tavern_task_definition.gd`
Added the CarriedItemRule enum and viability_weight.

### `systems/staff/tasks/tavern_task_service.gd`
Viability gate plus long-shot fallback in selection; carried-item compatibility check; same-worker reclaim cooldown; task-switch margin and commitment window; decision records; aggregate report sections; cancellation-reason counts; invested-time banking.

---

## Replaced

None. Every change above is an edit to an existing file or a new file;
no file was wholesale replaced with different content under the same name.

---

## Deleted manually

**No files need to be deleted.**

`DELETE_THESE_FILES.md` is therefore not included.

One *function pair* was removed inside a modified file —
`StaffMember._begin_returning_carried_item()` and `_find_free_service_slot()`,
superseded by `CarriedItemRecovery`. That is an edit, not a file deletion.

---

## Not included

Deliberately excluded, per the brief: `.godot/`, imported cache data,
editor state, temporary logs, diagnostic exports, and every unchanged
project file and asset.

`.uid` files are also excluded — Godot regenerates them on import, and
shipping stale ones alongside copied scripts risks a uid collision.

PHASE A - PART 1  |  base commit b1e36ba  |  14 files

MODIFIED (5)
------------
scripts/Entities/customer.gd
    + is_settled domain flag in get_activity_flags()
    + starts the visit clock from _on_reached_group_slot() and from the
      group branch of arrive_at_seat(), both via the idempotent variant

systems/customer_ai/customer_needs.gd
    + has_visit_clock_started()
    + ensure_visit_clock_started()  (idempotent - a member reaches its slot
      several times per visit and must not get a fresh visit each time)
    ~ update_remaining_visit_time() now reports the FULL duration when the
      clock has not started, instead of collapsing to zero

Data/customer_ai/activities/visit_tavern_activity.tres   is_seated -> is_settled,
                                                         + group cohesion conditions
Data/customer_ai/activities/socialise_at_seat.tres       is_seated -> is_settled
Data/customer_ai/activities/relax_at_seat.tres           is_seated -> is_settled,
                                                         not_currently_ordering ->
                                                         not_transacting_at_bar,
                                                         base_utility 6.0 -> 7.5

NEW - CONDITIONS (3)
--------------------
Data/customer_ai/conditions/is_settled.tres
Data/customer_ai/conditions/group_has_away_capacity.tres
Data/customer_ai/conditions/group_not_drinking_scoring.tres

NEW - TESTS (6)
---------------
tests/phase_a_gate_audit.gd / .gd.uid / .tscn
    Drives main.tscn and, every second, builds a real ActivityContext from
    each live customer's own brain and evaluates every activity's conditions
    INDIVIDUALLY, tallying which condition blocked what. Also reports state
    occupancy, drinks per customer, remaining-visit buckets and visit-clock
    state. This is the tool that found both root causes.

tests/phase_a_audit_probe.gd / .gd.uid / .tscn
    Narrower behaviour-mix sampler.

UNTOUCHED
---------
scenes/main/main.tscn is NOT in this package.
